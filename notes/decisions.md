# 의사결정 기록 (Decision Log)

> 기술 선택·설계 결정을 남긴다. 형식: **결정 / 이유 / 대안 / 날짜**

## D-001 · 대시보드 우선 개발
- **결정:** 하드웨어/AI 전에 대시보드 프론트엔드를 먼저 만든다(목 데이터 구동).
- **이유:** "무엇을 보여줄지"를 정하면 Jetson이 무엇을 추론·전송해야 하는지 스펙이 역으로 확정됨. 하드웨어 없이도 진도가 나가고 데모가 빠름.
- **대안:** AI 추론부터 개발 — 그러나 출력 스펙이 안 잡혀 대시보드와 계속 어긋날 위험.
- **날짜:** 2026-07-10

## D-002 · 대시보드 스택 = React + Vite
- **결정:** React + Vite로 웹 대시보드 구축.
- **이유:** 실시간 차트/UI 유연, 확장성 좋음, Pi에서 브라우저(키오스크)로 구동 가능.
- **대안:** Streamlit/Dash(빠르지만 커스터마이징 제한), Next.js(Pi에 과함).
- **날짜:** 2026-07-10

## D-003 · 통신 방식 = MQTT
- **결정:** Jetson ↔ Pi 통신은 MQTT. Pi에 Mosquitto 브로커, 브라우저는 MQTT-over-WebSocket으로 직접 구독.
- **이유:** 실시간 텔레메트리에 표준적으로 최적. pub-sub이라 로거/알람/액추에이터 추가가 쉬움(느슨한 결합). 별도 백엔드 서버 불필요.
- **대안:** WebSocket 직결(양방향 간단하나 다중 구독/확장 약함), REST 폴링(단순하나 실시간성 낮음).
- **날짜:** 2026-07-10

## D-004 · 개발 도구 = micromamba 사용자 영역 설치
- **결정:** 시스템 apt(sudo) 대신 micromamba env로 Node 20 + git을 사용자 영역에 설치.
- **이유:** 이 머신에서 sudo 비밀번호 없이 설치 필요. 재현성 있고 시스템 오염 없음.
- **대안:** apt/NodeSource(sudo 필요), nvm(node만).
- **날짜:** 2026-07-10

## D-005 · Jetson에서도 git 커밋/푸시 허용
- **결정:** Jetson 실기기에서 생성한 산출물(벤치 결과, 노트)은 Jetson에서 직접 커밋·푸시한다.
  단 작업 전 `git pull`을 먼저 해 히스토리 분기를 막는다. 무인(headless) 에이전트의
  main 커밋·push 금지 규칙은 그대로 유지(사람이 대화형으로 작업할 때만 해당).
- **이유:** 기존 "Jetson은 읽기 전용" 규칙은 히스토리 분기 방지 + 무인 규칙과의 일관성이 목적이었으나,
  실측 데이터를 dev 머신으로 수동 이동 후 커밋하는 것이 더 번거롭고 누락 위험이 큼.
- **대안:** 결과 파일 scp/USB 이동 후 dev 머신에서 커밋(수작업·누락 위험), Jetson 전용 브랜치(과함).
- **날짜:** 2026-07-13

## D-006 · 원시 데이터 저장 = Jetson 로컬(핫) + (옵션) 원격 아카이브(콜드)
- **결정:** 학습용 원시 데이터(RGB 이미지 2대·열화상·중심온도)는 **Jetson 로컬 SSD(500GB)에 1차 저장**한다.
  타임스탬프 매니페스트와 함께 기록. 장기 보관은 필요 시 **연구실 데스크탑으로 오프로드**(Tailscale rsync,
  야간·유휴 시간). 가벼운 텔레메트리(온도·doneness·상태값)는 어디 저장하든 무방.
- **이유:** 카메라·센서가 Jetson에 물리적으로 붙어 있어 데이터가 거기서 생성됨 — **소스에서 기록**이 원칙.
  풀프레임을 실시간으로 네트워크에 흘려 다른 박스에 저장하는 건 낭비이고, 링크 끊기면 세션 데이터 유실.
  MQTT는 대용량 바이너리 채널이 아님. "찍고 나서 오프로드"가 안전.
- **주의:** 500GB 단일 SSD = 백업 없음 → SSD 고장 시 데이터 전손. 핫→콜드 오프로드(또는 로컬 백업통)로
  사본 확보 필요. 오프로드는 데스크탑이 **다른 WiFi**라 인터넷 업로드 속도에 묶임 → 영상 용량 산정 후 주기 결정.
- **대안:** T470s를 1차 저장소로(소스 아님 → 네트워크 낭비), 데스크탑 실시간 저장(링크 유실 위험).
- **날짜:** 2026-07-24

## D-007 · 기기 역할 확정 — 시스템 3대 vs 보조 장비
- **결정:** **배포 시스템 = Jetson + Pi + 로봇(3대)**. 그 외는 시스템 밖 보조 장비로 취급.
  - **Jetson** — 카메라·AI 추론·원시 데이터 저장(발행자)
  - **Pi 5** — Mosquitto 브로커 + 대시보드 서빙 (현장 상설, 헤드리스)
  - **로봇** — 외주. MQTT 구독 클라이언트(자동 재연결). 브로커 IP+토픽+`shared/schema.json`만 전달, 내부는 그쪽 관할
  - **엘리트북** — 일상 업무·개발용(주력)
  - **T470s(띵크패드)** — 시스템 밖. 헤드리스 Pi의 **뷰어 겸 제어 단말**(브라우저로 대시보드, SSH로 Pi 관리)
    + (옵션) Jetson 데이터 **로컬 백업통**. 필수 아님 — 없어도 시스템 완결.
  - **연구실 데스크탑** — (옵션) 원격 아카이브 타깃(D-006). 다른 WiFi, Tailscale로 연결.
- **이유:** "현장에 상설 설치"가 가능한 건 작고 싼 임베디드 박스(Pi)뿐 — 노트북은 현장 상설 부적합.
  반대로 개발·뷰어·백업은 노트북이 맡으면 됨. 각 장비를 억지로 파이프라인에 끼우지 않고 역할을 분리해 단순화.
- **대안:** 브로커/대시보드를 Jetson에 통합해 Pi 제거(→ 현장 상설 디스플레이 장비가 없어짐. PoC 단계엔 가능하나
  현장 배포엔 Pi 필요), T470s를 데이터 서버로(D-006에서 기각).
- **날짜:** 2026-07-24

## D-008 · Pi 헤드리스 운영 + 대시보드는 웹 브라우저로 접속
- **결정:** Pi는 **모니터 없이 헤드리스**로 운영한다. 대시보드는 Pi가 웹으로 서빙하고, 보는 쪽은
  같은 네트워크의 **브라우저**(T470s·엘리트북·태블릿 등)로 `http://<Pi-IP>:포트` 접속. Pi 관리·개발은 **SSH**.
- **이유:** 대시보드가 웹이라 표시 장치가 Pi에 물릴 필요 없음. 노트북은 화면이 **출력 전용**이라 Pi의 HDMI를
  받아 물리 모니터가 될 수 없음 — 하지만 브라우저+SSH면 Pi 데스크톱 자체가 불필요. 현장 설치도 단순해짐.
- **대안:** Pi에 모니터 직결(현장 상설 키오스크 필요 시 별도 저가 모니터 연결은 가능, `docs/PI5_SETUP.md` §5),
  노트북을 Pi 물리 모니터로(불가 — 입력 없음), VNC(Pi GUI가 꼭 필요할 때만).
- **날짜:** 2026-07-24

## D-009 · 네트워크 = 로컬 스위치(실시간) + Tailscale(원격), 스위치는 공유기 업링크
- **결정:** 두 평면으로 분리한다.
  - **로컬 실시간** — Jetson↔Pi MQTT·MJPEG·로봇은 **기가비트 스위치(유선)**. 인터넷 끊겨도 조리 시스템은 동작.
  - **원격** — 연구실 데스크탑 아카이브·원격접속은 **Tailscale**(다른 WiFi 간).
  - 스위치는 **공유기(라우터)에 업링크**해 인터넷을 물린다 → NTP 시간동기 + Tailscale 연결 확보.
- **이유:** 실시간 조리 트래픽은 로컬 유선이라야 지연·안정성 확보(WiFi로 MJPEG 흘리면 끊김). 스위치 단독은
  인터넷·DHCP·NTP·Tailscale 불가 → 타임스탬프 정합/원격백업이 막힘. 공유기 업링크로 해결.
- **대안:** 스위치 단독(인터넷·NTP 불가), 전부 WiFi(실시간 영상 불안정), 로컬 NTP 서버만 운영(원격백업 불가).
## D-010 · 도네스 모델 아키텍처 확정
- **결정:** RGB 2뷰(EfficientNet-B0 ×2, 뷰별 독립) + MLX90640 열배열(2D-CNN+1D TCN+수작업 공간피처)
  **late fusion** + MLX90614 온도 스칼라 **gated fusion** → **CORN ordinal 3단계** 헤드 +
  boil_intensity 보조 헤드. 배포는 전 branch **TensorRT fp16**, 추론 1 Hz. 상세: `docs/model-architecture.md`.
- **이유:** 벤치 실측(487 FPS)으로 속도가 비제약임이 확인돼 백본을 정확도 기준으로 선택 가능.
  RGB↔32×24 열배열은 픽셀 정합이 안 되므로 early stacking 배제(조사 3차). 완료↔과조리는 온도 포화
  구간이라 gated fusion으로 시각 신호에 자동 이양. INT8은 efficientnet에서만 이득이라 캘리브레이션
  비용 대비 채택 안 함.
- **대안:** MobileNetV3-Small 백본(전력 필요 시 폴백), early fusion+guided SR(LapGSR — ablation A2에서
  이길 때만), TSM/MS-TCN 시계열 승격(프레임 분류 불안정 시), INT8 양자화(전력 부족 시).
- **날짜:** 2026-07-24

## D-011 · Pi에 모니터 직결 (D-008 개정)
- **결정:** Raspberry Pi 5에 모니터를 직결한다. D-008의 "헤드리스 운영"을 개정 — Pi가 장비 관리 PC
  역할(인계 플랜 §2)을 맡으면서 현장 상설 관리 화면이 필요해짐. SSH·타 기기 브라우저 접속은 그대로 병행.
- **이유:** 관리 화면(촬영 시작/종료·상태 감시·전원 조작)은 장비 옆에서 즉시 조작할 일이 많고,
  Jetson이 꺼진 상태에서도 보여야 함 → Pi 직결 모니터가 가장 단순·확실한 경로.
- **대안:** 헤드리스 유지 + 노트북 브라우저(D-008 원안 — 장비 옆 즉시 조작 불편), 태블릿 거치(전원·거치 관리 추가).
- **날짜:** 2026-09-11

## D-012 · 열화상 = 보유 MLX90640 그대로 사용
- **결정:** 실험 장치의 열화상은 신규 구매 없이 **보유 MLX90640 2종(55°/110°)을 그대로 사용**한다.
  인계 플랜의 "열화상 모델·인터페이스 미확정"은 이것으로 확정. FOV 선택은 프레임 장착 거리 확정 후
  (`docs/model-architecture.md` §2.0 계산법).
- **이유:** 플랜이 제안한 선정 조건(온도 원시 데이터 확보)을 MLX90640이 충족(768개 ℃ 원시값, I²C).
  이미 보유·문서화돼 있어 추가 비용·SDK 리스크 없음.
- **대안:** 고해상 열화상 추가 구매(FLIR Lepton 160×120 등) — 32×24 해상도가 부족하다고 실측으로
  판명될 때만 재검토.
- **날짜:** 2026-09-11

## D-013 · Pi 관리 서버 = Python + FastAPI, 화면은 무빌드 정적 페이지
- **결정:** Pi의 장비 관리 서버는 **Python 3 + FastAPI**(`pi-server/`), 설정·이력은 **SQLite**,
  상시 실행은 **systemd**(`soup-pi-server`, 포트 8100). 관리 **화면은 빌드 단계 없는
  정적 HTML/CSS/JS**(`pi-server/app/static/`)로 서버가 직접 서빙한다. 기존 React 대시보드
  (포트 3000)는 **조리 데이터 뷰어로 그대로 유지** — 둘을 합치지 않는다.
- **이유:** ① 관리 서버는 GPIO(전원 제어)·프로세스 관리·네트워크 프로브를 하게 되는데 전부
  Python 생태계가 가장 단순. ② 관리 화면의 존재 이유가 "**Jetson·네트워크가 죽어도 뜨는 화면**"
  이므로 의존성이 적을수록 좋다 — 정적 페이지는 npm 빌드·Node 런타임 없이 Pi 부팅 직후 뜬다.
  ③ 조리 대시보드는 MQTT 텔레메트리(Jetson 살아 있어야 의미 있음)를 보는 화면이고 관리 화면은
  Jetson이 죽었을 때 가장 필요하다 — **가용성 요구가 정반대라 한 앱에 합치면 서로를 끌어내린다.**
- **대안:** React 대시보드에 관리 페이지 추가(코드 재사용은 좋으나 npm 빌드·Node 의존이
  관리 화면의 가용성 요구와 충돌, 대시보드 배포와 결합됨), Flask(비동기 감시 루프·
  스키마 검증에 추가 작업 필요), Node/Express(Pi에서 GPIO·시스템 제어 시 Python보다 불리).
- **날짜:** 2026-09-11

## D-014 · 제어 채널은 MQTT가 아닌 HTTP API (텔레메트리와 분리)
- **결정:** Pi 관리 서버 ↔ Jetson 수집 서비스의 **명령·상태 조회는 HTTP**(`docs/pi-jetson-api.md`).
  Jetson이 서버, Pi가 클라이언트. 기존 MQTT(`cooking/status`)는 **조리 텔레메트리 방송용으로 유지**.
- **이유:** 촬영 시작/중지는 "받아들여졌는지, 거절 사유가 뭔지"를 그 자리에서 받아야 하는
  요청-응답이다. MQTT로 하려면 요청/응답 토픽 쌍 + 상관관계 ID를 직접 만들어야 해서 더 복잡해진다.
  또 브로커(Pi)가 죽으면 제어까지 막히는데, 제어는 브로커와 무관해야 한다.
- **대안:** MQTT 요청/응답 토픽(구현 복잡·타임아웃 처리 직접 구현), gRPC(스키마 도구 부담),
  SSH 원격 명령(권한·감사 이력 관리가 어려움).
- **날짜:** 2026-09-11

## D-015 · 전원 제어는 "미지원"이 기본값 — 근거 없는 상태 표시 금지
- **결정:** 전원 제어 회로가 없으므로 관리 서버의 전원 기능은 **기본값 `unsupported`**로 두고,
  조작 요청은 **501**로 거절하며 화면에도 사유를 표시한다. 모의 조작은 `SOUP_POWER_MODE=mock`
  에서만 가능하고 응답·화면에 항상 `simulated: true`가 붙는다. 또한 상태 판정에서
  **"전원 꺼짐"은 전원 제어기가 OFF를 보고할 때만** 쓰고, 프로브 실패는 원인 미상
  **"연결 끊김"**으로만 표시한다(전원 OFF와 네트워크 단절을 구분할 수 없으므로).
- **이유:** 인계 플랜 §5의 "하드웨어가 없을 때 실제 전원을 제어하는 것처럼 보이게 만들지 않는다"를
  코드 기본값으로 못 박은 것. 관측(프로브 결과)과 추정(전원 상태)을 섞으면 나중에 실제 회로가
  붙었을 때 오작동을 잡아내기 어렵다.
- **대안:** 버튼을 그냥 두고 동작만 막기(오해 소지), 프로브 실패=전원 꺼짐으로 표시(네트워크
  단절 시 잘못된 판단 유도 — 플랜 §5가 금지한 "ping 실패 하나로 전원 차단" 경로의 시작).
- **날짜:** 2026-09-11

## D-016 · 기록 스키마 v2 — 열 추가 방식 확장 + 실험 기록은 보존 정책 제외
- **결정:** Pi 기록 스키마에 버전(`schema_version=2`)을 도입하고, 확장은 **`ALTER TABLE`
  열 추가만** 한다(기존 DB를 지우거나 재생성하지 않음). 보존 정책(자동 정리)의 대상은
  **세션과 무관한 운영 로그와 `host_metrics`뿐**이며, `sessions`와 **`session_id`가 붙은
  사건은 절대 자동 삭제하지 않는다**.
- **이유:** 실기기에 이미 운영 기록이 쌓여 있어 재생성은 손실이다. 또 "로그 정리"가
  연구 기록을 지우는 사고를 구조적으로 막아야 한다 — 정책이 아니라 쿼리 수준에서
  `session_id IS NULL` 조건으로 못 박았다. 내보내기 파일에도 `schema_version`을 실어
  나중에 해석 가능하게 한다.
- **대안:** 스키마 재생성 후 마이그레이션 스크립트(과함·위험), 보존 정책을 운영자 판단에
  맡기기(사고 시 복구 불가), 별도 아카이브 테이블로 이동(복잡도만 증가).
- **날짜:** 2026-09-12

## D-017 · 사건은 하나의 `events`로 통합하고 시각을 3종으로 구분
- **결정:** 조작·상태변화·오류·**수동 입력**(재료 투입·가열·교반·메모)을 별도 테이블로
  나누지 않고 `events` 한 곳에 모으되, `origin`(pi/jetson/manual)으로 구분한다. 시각은
  **`ts`(Pi 기록 시각) / `occurred_at`(실제 발생 시각) / `detail.sent_at`·`confirmed_at`
  (명령 보낸 시각·확인 시각)** 으로 나눠 저장하고, 한 조작의 사건들은 `request_id`로 묶는다.
  경과시간은 벽시계가 아닌 **monotonic**으로 재고, **기기 간 monotonic 비교는 금지**한다.
- **이유:** 조회·내보내기를 한 번에 하려면 한 테이블이 유리하다(실험 1건 = `session_id`
  필터 하나). 시각을 하나로 뭉개면 "언제 일어났는지"와 "언제 알았는지"가 영원히 구분
  불가능해진다 — 사후 입력과 장치 보고가 섞이는 이 장치에서는 치명적이다.
  `occurred_at`을 모르면 `ts`를 복사하지 않고 `null`로 둔다(모른다는 사실도 기록이다).
- **대안:** `marks` 별도 테이블(조회·내보내기 이중화), 시각 단일 필드(정보 손실),
  전역 시퀀스 없이 사건 나열(요청-응답 대응을 사람이 추측해야 함).
- **날짜:** 2026-09-12

## D-018 · "저장 완료"는 Jetson 요약 수신으로만 확정, 조작 출처는 신원을 지어내지 않음
- **결정:** ① 중지 요청 응답만으로 저장 완료로 기록하지 않는다. Jetson의
  `last_session_summary`(저장 결과 요약)를 받아야 `capture.save_confirmed`를 남기고
  세션에 박제한다. 요약이 없으면 화면·내보내기에 **`미확인`** 으로 남는다.
  ② 인증 기능이 없으므로 조작 사건에 **사용자 신원을 만들지 않는다** — `source`는
  `ui`/`api`, `detail`에 `client_host`와 `authenticated:false`만 남긴다.
- **이유:** ① "요청 성공"과 "실제로 저장됨"은 다른 사실이다. 둘을 같게 적으면 데이터가
  없는데 있다고 믿는 상태가 만들어진다(플랜 §4의 명시 요구). ② 로그인 없이 이름을
  적어 두면 나중에 그 기록이 증거로 쓰일 때 거짓이 된다. 관측 가능한 것만 적는다.
- **대안:** 중지 응답을 저장 완료로 간주(간단하지만 사실과 다름), 화면에서 사용자 이름
  입력받기(인증이 아니므로 위조 가능 — 신뢰할 수 없는 기록이 됨).
- **날짜:** 2026-09-12
## D-019 · 음향(마이크) 센서는 수집 장치에 넣지 않는다
- **결정:** 마이크(USB/I2S/접촉식 피에조 모두)를 수집 장치 센서 구성에서 **제외**한다.
  끓음 신호가 필요하면 기존 계획대로 탑뷰 RGB(거품·증기)와 열화상으로 얻는다.
- **이유:** 4차 방법론 조사(`docs/research-methodology-4-acoustic.md`) 결과 ①소리는 끓음 강도만
  주고 **완료/과조리(doneness)는 못 준다** ②검증된 성능은 전부 물·단일 용기·실험실(하이드로폰/접촉식)
  기준이고 공기 중 마이크는 감쇠·잡음이 명시된 약점 ③급식 주방의 **복수 솥 동시 조리·후드 소음**에서
  어느 솥 소리인지 분리가 안 돼 라벨 불가 데이터가 될 위험이 큼. 하드웨어·연산 비용은 작지만
  얻는 정보가 기존 센서와 겹치고 리스크만 추가된다.
- **대안:** USB 마이크 원시 WAV만 저장해 두고 8단계에서 판단(저비용이지만 소스 분리 문제는 그대로),
  접촉식 피에조를 솥에 부착(SNR 좋으나 내열·장착·세척 문제), I2S MEMS(JetPack 6 DTB 병합이
  카메라 오버레이와 충돌 위험).
- **날짜:** 2026-09-11

## D-020 · GMSL 카메라 캡처는 OpenCV가 아니라 V4L2 ioctl 직접 호출
- **결정:** ISX031F 어댑터는 `cv2.VideoCapture` 대신 ctypes로 V4L2(mmap 스트리밍)를 직접 쓴다
  (`jetson/collector/app/sensors/v4l2dev.py`). 구조체 크기·오프셋은 aarch64 커널 헤더로 검증.
- **이유:** 2026-09-12 실측에서 OpenCV V4L2 백엔드는 링크가 끊긴 카메라에서도 `ok=True`로 전부 0인
  프레임을 100fps로 돌려주고 `POS_MSEC`=0, 노출·게인=−1을 반환했다 → 유효성·누락·시각의 근거가 없다.
  직접 DQBUF하면 `v4l2_buffer.sequence`(하드웨어 순번 → 누락 감지 근거), monotonic 타임스탬프
  (`device_ts.clock=host_monotonic`), `V4L2_BUF_FLAG_ERROR`를 얻는다. 연구용 기록의 "누락을 근거 없이 0으로
  단정하지 않는다" 요구를 만족하려면 이 정보가 필수다.
- **대안:** OpenCV(정보 부족), GStreamer `v4l2src`(타임스탬프는 있으나 시퀀스·플래그 접근 불편, 파이프라인 의존),
  `python-v4l2` 패키지(미설치·유지보수 정체).
- **날짜:** 2026-09-12

## D-021 · 세션 저장 레이아웃 — 프레임 파일 + 배열 레코드 + JSONL 인덱스, 버린 샘플도 인덱스에 남김
- **결정:** `<root>/<session_id>/` 아래 `session.json`(메타)·`events.jsonl`·`stats.jsonl`·`manifest.json`,
  스트림별 `index.jsonl` + 이미지는 `frames/NNNNNN.jpg`, 수치 배열(depth uint16 mm, thermal float32 ℃)은
  `records.bin`(고정 크기 레코드, 인덱스에 offset·bytes). 대기열 초과로 **버린 샘플도 인덱스에 `path:null`,
  `invalid_reason:writer_queue_full`로 남긴다.** `stopped`는 flush·close·manifest 기록 후에만 보고한다.
  프로세스 중단으로 열린 채 남은 세션은 기동 시 `failed(interrupted)` + 파일 `partial`로 닫는다.
- **이유:** 연구용 원본은 "무엇을 받았고 무엇을 저장했는지"가 프레임 단위로 재구성돼야 한다. 프레임 파일은
  분석·라벨링 도구가 바로 읽고, 배열은 미리보기 이미지가 원본 수치를 대체하지 않도록 이진 그대로 둔다.
  JSONL은 append-only라 중단돼도 앞부분이 살아남고 프레임마다 DB 동기 쓰기가 없다.
- **대안:** 동영상 컨테이너(mp4/mkv) — Orin Nano는 NVENC가 없어 CPU 인코딩이며 프레임 단위 메타 결합이
  약함(실측 후 7단계에서 재검토), SQLite 인덱스(동기 쓰기 부담), HDF5(부분 손상 시 복구 어려움).
- **날짜:** 2026-09-12

## D-022 · Jetson 센서 수집은 단독 진단 후 기존 서비스에 통합
- **결정:** MLX90640 2대, MLX90614 2대, MAX31865/PT100 1대의 어댑터와
  단독 진단을 먼저 검증하고, 운영 시에는 기존 `jetson/collector` 서비스가 저장·상태를 관리한다.
- **이유:** 두 I²C 센서 쌍은 주소가 각각 겹쳐 mux 채널 식별이 필요하고, 기존 Python 파일은
  Raspberry Pi용 단일 센서 데모다. 센서별 고장과 1.5 m 배선 문제를 구분한 뒤 통합해야
  데이터의 `sensor_id`·시각·오류를 일관되게 남길 수 있다.
- **대안:** 처음부터 5대를 한 파일에서 구현(고장 원인 분리 어려움), 센서마다 별도 운영
  프로세스 유지(같은 I²C mux의 채널 변경 조정이 복잡함), 별도 수집 서비스 신설
  (기존 세션·저장·Pi 계약 기능 중복).
- **날짜:** 2026-09-18

## D-023 · Gemini 2는 Jetson 수집 서비스의 SDK 어댑터로 실행

- **결정:** Gemini 2를 Jetson USB에 연결하고 `jetson/collector`의 `cam_depth_0` 어댑터가
  Orbbec Python SDK v2로 color·depth·IR을 수집한다. 색상은 프레임별 JPEG(선택 시 BGR raw),
  깊이는 SDK Z16 값과 scale을 적용한 mm `uint16`, IR은 Y8/Y16 강도를 `uint16` 배열로 저장한다.
  Pi는 기존 API로 세션을 제어하며, 미리보기는 활성 세션에서 요청한 경우에만 Jetson이
  제한된 크기·속도의 JPEG로 제공한다. 장치 탐색과 실물 검증 결과를 구분한다.
- **이유:** 원본 파일·시각·장치 설정·누락 통계를 기존 Jetson 세션 기록과 같은 방식으로
  관리할 수 있고, Pi에 USB 수집 부하나 별도 원본 경로를 만들지 않는다. 미리보기는
  수집 루프의 이미 받은 샘플로 생성해 카메라를 중복으로 열지 않는다.
- **대안:** Pi에서 SDK 실행(원본과 제어의 역할 분리 훼손), 별도 Gemini 수집 프로세스
  (세션·저장 기록 중복), 미리보기용 두 번째 장치 연결(독점 접근 충돌).
- **날짜:** 2026-09-18

## D-024 · I²C 센서는 Adafruit 드라이버 + 프로세스 내 버스 잠금, 설정 미확정 값은 기본값 없음
- **결정:** MLX90640·MLX90614·MAX31865는 Adafruit CircuitPython 드라이버(Blinka, `ExtendedI2C`,
  `adafruit_tca9548a`)로 읽고, 같은 버스의 어댑터는 `i2cmux.py`의 버스별 잠금으로 mux 채널 선택~읽기
  완료를 직렬화한다. 버스 번호·MAX31865 CS 핀·기준 저항은 코드 기본값을 두지 않고 환경설정으로만 받는다.
  읽기 실패·fault는 `valid:false` 샘플로 기록하고, I/O 연속 실패(기본 5회)만 재연결로 승격한다.
- **이유:** 드라이버의 잠금은 트랜잭션 단위라 열화상 한 프레임(트랜잭션 수십 번) 도중 다른 스레드가
  mux를 바꿀 수 있다. 확인 안 된 `i2c-1`/430 Ω 같은 기본값은 "되는 것처럼 보이는 잘못된 데이터"를 만든다.
  실패를 기록으로 남겨야 1.5 m 배선의 오류율을 세션 데이터에서 바로 셀 수 있다.
- **대안:** 커널 `i2c-mux-pca954x` 하위 버스(장치 트리 오버레이 필요 — 카메라용 DT와 충돌 검토 부담,
  수동 제어와 혼용 금지), `smbus2` 직접 구현(MLX90640 보정 계산을 다시 써야 함), 센서별 프로세스(D-022에서 기각).
- **날짜:** 2026-09-18

## D-025 · MLX90640 초기 설정: 목표 2 Hz, 장치 refresh 8 Hz
- **결정:** 카메라당 목표 전체 프레임 2 Hz, 장치 refresh rate 8 Hz로 시작한다(둘 다 환경설정·세션 설정으로 변경 가능).
- **이유:** 전체 프레임 1장은 서브페이지 2장이고, 같은 버스의 두 카메라는 직렬로 읽는다. 읽기 1회가
  버스를 약 `2/refresh`초 쥐므로 2 Hz×2대에는 refresh ≥ 8 Hz가 필요하다(8 Hz면 약 0.25 s × 2대 × 2 Hz = 버스 점유 100%에 근접 — 여유 없음).
  실측해서 못 미치면 refresh 16 Hz(잡음 증가) 또는 목표 주기 하향 중에서 정한다.
- **대안:** refresh 2 Hz(전체 프레임 1초 — 두 대면 각 0.5 Hz), 16 Hz 이상(잡음 증가, 400 kHz에서 전송 시간 비중 커짐).
- **날짜:** 2026-09-18 (실물 검증 전 계산값)

## D-026 · Gemini 2 기본 수집 주기는 10 fps
- **결정:** 세션 설정에 `fps`가 없으면 Gemini 2의 color·depth·IR을 10 fps로 연다
  (`COLLECTOR_ORBBEC_FPS=10`). 세션에서 `fps` 또는 `per_sensor.cam_depth_0.fps`를 주면 그 값이 우선한다.
  fps만 바꿀 때는 SDK 기본 포맷·해상도(color 1280×720 MJPG, depth/IR 1280×800)를 유지한다.
- **이유:** 실기기 실측에서 30 fps는 depth+IR(각 1280×800 uint16)만 약 115 MB/s — 30분 세션에 약 200 GB로
  여유 공간(438 GB)의 절반이다. 10 fps는 약 40 MB/s(15초 592 MB 실측)이고, 국/탕 조리의 변화 속도에는
  10 fps로 충분하다고 판단했다(사용자 결정). 8회 연속 세션에서 세 스트림 모두 9.99 fps, 드롭 0.
- **대안:** 30 fps 유지(저장 공간·쓰기 부하), 5 fps(여유는 크지만 끓음·거품 같은 빠른 변화 관찰에 불리),
  해상도 축소(깊이 정밀도 손실), IR을 uint8로 저장(절반 절감 — 별도 검토 과제로 남김).
- **날짜:** 2026-09-18

## D-027 · Pi 카메라 미리보기는 기존 관리 화면에 끼우는 독립 컴포넌트 + Pi 중계
- **결정:** 카메라 미리보기(GMSL2 ①·②, Gemini 2 Color/Depth/IR)는 새 대시보드를 만들지 않고 기존 Pi 관리 화면에
  끼워 넣는 의존성 없는 JS 컴포넌트(`CameraPreview.mount(el, {apiBase, cameras, ...})`)로 만든다. 카메라 목록·API 주소는
  설정으로만 받고(`GET /api/preview/config` ← `SOUP_PREVIEW_CAMERAS`), 그림은 Pi 서버가 Jetson의 기존
  `capture/preview`(저속 JPEG)를 **그대로 중계**한다. 스트림 전환은 화면에서 어떤 주소를 읽을지만 바꾸고 Jetson 수집 설정은 건드리지 않는다.
  화면에서 사라지면(IntersectionObserver·탭 숨김·세션 없음) 주기 요청을 멈춘다.
- **이유:** 촬영·원본 저장의 책임을 Jetson 수집기 한 곳에 두어야 미리보기가 기록을 망가뜨릴 수 없다. 브라우저는 Pi↔Jetson
  직결망(10.42.0.x)에 닿지 못할 수 있어 Pi 중계가 필요하다. 관리 화면은 무빌드 정적 페이지(D-013)이고 재설치된 Pi에는
  node가 없어, 빌드가 필요 없는 컴포넌트가 가장 적은 비용으로 어디에나(React 대시보드 포함 — ref에 mount) 끼울 수 있다.
  Jetson이 최대 2 fps로만 갱신하므로 폴링(기본 1초)으로 충분하고 MJPEG 스트림 같은 새 데이터 경로가 필요 없다.
- **대안:** 브라우저가 Jetson에 직접 접속(직결망·CORS 문제, Jetson 주소가 화면에 박힘), MJPEG/WebRTC 스트리밍(Jetson에 새
  경로·부하 — 원본 수집과 경합), React 대시보드에 구현(Pi에 빌드 환경 없음, 관리 화면과 이원화),
  status의 센서 목록에서 카메라를 자동 발견(계약 확장 필요 — 설정 주입으로 충분해 철회).
- **날짜:** 2026-09-18

## D-028 · 열화상과 MLX90614는 I²C 버스를 나눈다 (실측 확정)
- **결정:** 배선안대로 **열화상(MLX90640) = J12 3/5번 = `/dev/i2c-7`(400 kHz)**,
  **비접촉 온도(MLX90614) = J12 27/28번 = `/dev/i2c-1`(100 kHz)** 로 버스를 분리한다.
  원격 센서부까지 4심 케이블 2가닥이 필요하다는 뜻이며, "4심 1가닥 + mux 1개" 대안은 철회한다.
- **이유:** 같은 MLX90640 1대를 두 버스에서 실측했다(2026-09-23, mux 없이 직결).
  400 kHz·refresh 8 Hz에서 취득 158 ms(3분 360/360 성공, 실효 2.00 Hz)인데,
  100 kHz에서는 refresh 8 Hz가 **전부 실패**(`getFrame()` data-ready 초과)하고 refresh 4 Hz로 낮춰야 동작하며
  취득이 **508 ms로 3.2배**가 된다. 열화상 2대를 100 kHz 한 버스에 두면 초당 4회 × 0.51초 = 2.04초가 필요해
  각 2 Hz(D-025)가 물리적으로 불가능하다(상한 각 ~0.95 Hz). 400 kHz면 0.63초/초로 여유가 있다.
  여기에 MLX90614 2대를 더하면 더 나빠진다.
- **대안:** ① 4심 1가닥·100 kHz 공유 버스(열화상을 각 ~0.95 Hz로 낮추면 성립 — 도네스 판정에 그 주기가 충분한지
  확인되지 않아 철회) ② MLX90614를 100 kHz 이상으로 운용(규격 위반) ③ 열화상 refresh 16 Hz로 올려 보정
  (잡음 증가, 100 kHz에서는 전송 시간이 병목이라 무의미).
- **날짜:** 2026-09-23 (실측 근거: `notes/data/experiments/20260923_mlx90640-first-real-read.md`)

## D-029 · MLX90614(적외선) 우선순위 하향, PT100을 먼저 검증한다
- **결정:** 센서 검증 순서를 **열화상 → PT100 → (mux 도착 후) 적외선**으로 바꾼다. MLX90614는 D-010의
  온도 스칼라 gated fusion 입력으로 계획에 남기되, 지금 단계에서 진행을 막는 요소로 두지 않는다.
  2026-09-23에 MLX90614 1대가 `i2c-1`에서 한 번도 응답하지 않아(주소 `0x5A` 무응답, 버스 timeout) 원인 미확정 상태다.
- **이유:** 절대 온도 기준값은 **접촉식 PT100**이 담당하고(GT 정의에 직결), 솥 중심 온도는 열화상 중심 화소로도 얻는다.
  MLX90614의 고유 강점은 절대 정확도 ±0.5 ℃(열화상은 ±2 ℃급)와 좁은 FOV지만, 도네스 판정은 절대값보다
  분포·시간 변화를 쓰므로 일정한 절대 오차는 치명적이지 않다. 적외선 2대는 어차피 mux가 있어야 동시 운용되고,
  PT100은 SPI라 이번 I²C 문제와 무관하게 진행할 수 있다.
- **2026-09-23 적외선 측정 기록(원인 규명용):** 모듈 연결 시 J12 27번이 3.3 → 1 V로 끌려가고 모듈 쪽 SDA·SCL은 0 V,
  모듈을 떼면 27번이 3.3 V로 복귀하고 버스 오류 0건. 즉 **모듈 쪽이 신호선을 끌어내린다**. 남은 후보는
  **① 모듈 SDA/SCL↔GND 단락(또는 구멍 오인)** 하나만 남았다.
  (당초 후보였던 "② `ESF-BCC`가 5 V 타입인데 3 V로 구동"은 **데이터시트로 반증됐다** — `MLX90614Bxx/Dxx`는
  **3 V 타입**(VDD 2.6/3.0/3.6 V, 절대 최대 5 V)이고 5 V는 `Axx`다. 3.0 V는 규격 안, 오히려 표준값이다.)
  그 오판으로 5 V 시험을 했고, 전원이 열화상 모듈에서 점프되는 배선이라 **열화상에도 5 V가 인가**됐다
  (열화상 정격 최대 3.6 V). 3.3 V 복귀 후 열화상은 `0x33`·ID 정상. 적외선도 절대 최대 5 V라 즉시 파괴 수준은 아니지만
  규격 밖 인가였다. **적외선·열화상 모두 3.3 V로만 다루고, 전원은 모듈 간 점프 금지·헤더에서 각각 인출**한다.
- **대안:** 적외선을 계획에서 완전히 제거(D-010 구조 변경 필요 — 데이터 확보 후 ablation으로 판단하는 게 맞아 보류),
  지금 모듈 교체·주소 변경까지 밀어붙이기(mux 도착 전이라 어차피 2대 동시 운용 불가).
- **날짜:** 2026-09-23
