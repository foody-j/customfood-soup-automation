# dependencies
node_modules/
dashboard/node_modules/

# build output
dist/
dashboard/dist/

# logs
*.log
npm-debug.log*

# env
.env
.env.local
.env.*.local

# editor / OS
.vscode/
.idea/
.DS_Store

# 대용량 원시 데이터(수집 영상/이미지 등)는 git에 올리지 않음 — notes/data/README.md 참고
notes/data/raw/

# 벤치마크가 export하는 ONNX (재생성 가능, 커밋 불필요)
notes/data/bench/onnx/

# Python
__pycache__/
*.pyc
cf-venv/
.venv/
.pytest_cache/

# Pi 관리 서버 런타임 데이터(SQLite 세션·이벤트 이력) — 기기별 로컬 상태
pi-server/data/



# 3D 모델(26MB) — repo 제외, 디스크엔 유지
cam-adaptor/FG12-4CH/FG12_4CH_3DModel.stp

# Orbbec SDK가 실행 디렉터리에 만드는 로그
jetson/collector/Log/
